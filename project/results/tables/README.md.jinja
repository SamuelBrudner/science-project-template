# `results/tables/` — Terminal report tables

Use this directory for generated summaries or model outputs consumed by people
or reporting artifacts, not as reusable analysis inputs. A dataset consumed by
another stage or analysis belongs in `data/processed/` and is named in
`conf/catalog.yaml`.

These files are ignored and regenerated. See the root
[`README.md`](../../README.md) contract.
