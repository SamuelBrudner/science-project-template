# Thin Snakemake config: DAG selectors/topology only.
#
# Dataset locations and formats live in conf/catalog.yaml. Scientific parameters
# live in conf/config.yaml. Executor resources live in workflow/profiles/.

{%- if include_example %}
# Select the logical catalog entry used as the sample manifest. Override the key,
# not a filesystem path: snakemake --config sample_manifest_dataset=my_manifest
sample_manifest_dataset: "sample_manifest"
{%- else %}
{}
{%- endif %}
