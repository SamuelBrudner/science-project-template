# Copier answers — DO NOT EDIT BY HAND.
# Records the template version + every active answer so `copier update` can
# re-apply improvements without silently changing project policy. This includes
# authority/linkage choices such as bench records and Lab Tracker. Commit this
# file; change choices through Copier rather than editing this YAML by hand.
{{ _copier_answers | to_nice_yaml | trim }}
