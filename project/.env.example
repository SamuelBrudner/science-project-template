# Copy to .env (git-ignored) and fill in real values.
# Loaded by src/<pkg>/settings.py via pydantic-settings. Missing REQUIRED keys
# fail loud at startup. Never commit the real .env.

# Example secrets — replace with what this project actually needs:
# DATA_API_TOKEN=
# VAULT_ADDR=
# VAULT_TOKEN=
